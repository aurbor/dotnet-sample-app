apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: weather-api-base

# Resources generated by Helm template
resources:
  - all.yaml

labels:
  - includeSelectors: true
    pairs:
      managed-by: kustomize
